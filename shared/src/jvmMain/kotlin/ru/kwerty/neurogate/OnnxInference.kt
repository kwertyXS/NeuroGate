package ru.kwerty.neurogate

class OnnxInference(modelPath: String) {
    fun predict(features: FloatArray): String {
        // Это заглушка. Реализуйте здесь логику предсказания.
        return "Benign"
    }

    fun close() {
        // Освободите ресурсы, если это необходимо.
    }
}
